import React from 'react';
import Screen from '../Screen/Screen.jsx';

const styles = {
  main: {
    position: 'relative',
  },
};

export default () => (
  <div style={styles.main}>
    <Screen />
  </div>
);
